import { CreateTimerAction, Task } from "./classes";
export declare class TimerTask extends Task {
    isCompleted: boolean;
    isFaulted: boolean;
    action: CreateTimerAction;
    result: any;
    timestamp: Date;
    id: number;
    constructor(isCompleted: boolean, isFaulted: boolean, action: CreateTimerAction, result?: any, timestamp?: Date, id?: number);
    readonly isCanceled: boolean;
    cancel(): void;
}
