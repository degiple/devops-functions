import { ActionType, IAction, RetryOptions } from "./classes";
export declare class CallSubOrchestratorWithRetryAction implements IAction {
    functionName: string;
    retryOptions: RetryOptions;
    input: any;
    instanceId: string;
    actionType: ActionType;
    constructor(functionName: string, retryOptions: RetryOptions, input: any, instanceId?: string);
}
