import { ActionType, IAction } from "./classes";
export declare class CallActivityAction implements IAction {
    functionName: string;
    input: any;
    actionType: ActionType;
    constructor(functionName: string, input?: any);
}
