"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const classes_1 = require("./classes");
class TimerTask extends classes_1.Task {
    constructor(isCompleted, isFaulted, action, result, timestamp, id) {
        super(isCompleted, isFaulted, action, result, timestamp, id);
        this.isCompleted = isCompleted;
        this.isFaulted = isFaulted;
        this.action = action;
        this.result = result;
        this.timestamp = timestamp;
        this.id = id;
    }
    get isCanceled() {
        return this.action && this.action.isCanceled;
    }
    cancel() {
        if (!this.isCompleted) {
            this.action.isCanceled = true;
        }
        else {
            throw new Error("Cannot cancel a completed task.");
        }
    }
}
exports.TimerTask = TimerTask;
//# sourceMappingURL=timertask.js.map