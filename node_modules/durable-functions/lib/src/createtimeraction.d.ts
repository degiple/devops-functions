import { ActionType, IAction } from "./classes";
export declare class CreateTimerAction implements IAction {
    fireAt: Date;
    isCanceled: boolean;
    actionType: ActionType;
    constructor(fireAt: Date, isCanceled?: boolean);
}
