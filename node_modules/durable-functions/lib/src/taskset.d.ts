import { IAction } from "./classes";
export declare class TaskSet {
    isCompleted: boolean;
    actions: IAction[];
    result: any;
    constructor(isCompleted: boolean, actions: IAction[], result?: any);
}
