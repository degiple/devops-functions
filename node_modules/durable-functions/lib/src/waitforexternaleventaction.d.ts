import { ActionType, IAction } from "./classes";
export declare class WaitForExternalEventAction implements IAction {
    externalEventName: string;
    actionType: ActionType;
    constructor(externalEventName: string);
}
