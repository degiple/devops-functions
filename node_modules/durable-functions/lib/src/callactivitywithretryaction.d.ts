import { ActionType, IAction, RetryOptions } from "./classes";
export declare class CallActivityWithRetryAction implements IAction {
    functionName: string;
    retryOptions: RetryOptions;
    input: any;
    actionType: ActionType;
    constructor(functionName: string, retryOptions: RetryOptions, input?: any);
}
